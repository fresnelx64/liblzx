#pragma once

#ifndef LZX_MEM_7903192103161108	1
#define LZX_MEM_7903192103161108

#ifdef (__cplusplus)
	#include <cstdlib.h>
	#include <cstring.h>
#else
	#include <stdlib.h>
	#include <string.h>
#endif

ifdef (__cplusplus)
	extern "C" {
#endif

static void* LZX_malloc(size_t s) { return malloc(s); }
static void  LZX_free(void* p) { free(p); }
static void* LZX_memcpy(void* dest, const void* src, size_t size) { return memcpy(dest, src, size); }

#ifdef (__cplusplus)
	}
#endif

#endif

























































































































































































































































































































































































































































































